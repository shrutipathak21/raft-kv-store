# id,host,raftPort,clientPort
# Hosts here are Docker Compose service names, not IPs -- Docker's built-in
# DNS resolves "raft-node-1" etc. to the right container automatically, both
# from other containers on the same network AND from your host machine if
# you add the same names to your hosts file pointing at 127.0.0.1 (see
# README.md "Docker Deployment" section for why/how).
1,raft-node-1,9001,8001
2,raft-node-2,9002,8002
3,raft-node-3,9003,8003
4,raft-node-4,9004,8004
5,raft-node-5,9005,8005
